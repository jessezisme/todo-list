var App =   App || {};
App.Collection || (App.Collection = {});
App.Model       || (App.Model     = {});
App.View       || (App.View       = {});
App.Router     || (App.Router     = {});

var app = app || {};
app.collection = app.collection || {};
app.model = app.model || {}; 
app.view  = app.view || {};
app.router = app.router || {}; 
